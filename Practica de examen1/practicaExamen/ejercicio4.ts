/*Escribe una función que tome dos arreglos como parámetros y devuelva un nuevo arreglo que contenga los 
elementos de ambos arreglos concatenados. */

let numero1: number[] = [1,2,3,4]; 
let numero2: number[] = [1,2,3,4,8]; 
let arregloConcatenado: number [];

function concatenarArreglos (numero1:number[], numero2:number[]):number{

    return numero1.concat(numero2);
    
}

numeroMayor = numeroMasGrande(arregloNum);
console.log(numeroMayor);